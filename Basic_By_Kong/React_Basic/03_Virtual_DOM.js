/*
Virtual DOM
    การทำงาน
        Virtual DOM มีลักษณะคล้ายๆ กับ DOM ใน HTML โดย Virtual DOM จะทำการคัดลอก DOM จริง (Real DOM) มาเก็บลงใน Memory ถ้ามีารเปลี่ยนแปลงเกิดขึ้น Component ก็จะอัพเดตเฉพาะ Component ที่เปลี่ยนแปลงเท่านั้น โดยไม่จำเป็นต้องอัพเดต DOM ใหม่ทั้งหน้า ทำให้เกิดการทำงานได้รวดเร็ว
        
        ***ถ้าใช้ DOM แบบปกติจะ Refresh ทั้งหน้าเพื่ออัพเดตหน้าเว็บที่เปลี่ยนแปลงไป
*/